sqrt(var(math)*(length(math)-1)/length(math))
# 幾何平均
x <- c(1.2, 1.1, 1.05)
prod(x)^(1/length(x))
# 調和平均
v <- c(60, 20)
1/mean(1/v)
1/v
# 1.5 階級数の決め方
# スタージェスの公式
hist(math, breaks = 'Sturges')
# スコットの選択法
hist(math, breaks = 'scott')
# フリードマン＝ダイアコニスの選択法
hist(math, breaks = 'FD')
# 区切り（階級）を指定
hist(math, breaks = 18)
# 区切り（0から100まで10刻み）を指定
hist(math, breaks = seq(0, 100, 10))
# 1.6 Rのグラフをファイルに変換する
# 画像の書き出し
jpeg('math.jpeg', width = 640, height = 480)
hist(math)
dev.off()
# 1.7 分位点と箱ひげ図
# 中央値、メディアン（n=奇数）
score <- c(56, 78, 90)
median(score)
# 中央地、メディアン（n=偶数）
score <- c(56, 78, 81, 90)
median(score)
# 分位点
quantile(math)
# 四分位範囲（IQR）
IQR(math)
# 箱ひげ図
boxplot(math)
# irisデータの表示
iris
# データの先頭部分を表示
head(iris)
# データの末尾を表示
tail(iris)
# データの構造を表示
str(iris)
# 箱ひげ図の表示（１〜３列）
boxplot(iris[, 1:3])
# 箱ひげ図の表示（すべて）
boxplot(iris)
# 箱ひげ図の表示
boxplot(iris[, c(1, 3, 4)])
# 1.8 モード
# モードの表示
names(which.max(table(math)))
# table()について→出現回数の表示
table(math)
# which.max(x)→xの中で最大の値の”位置”を返す
mx <- c(10, 20, 30, 40, 50)
which.max(mx)
# 文字列データで使用
x <- c('Alice', 'Bob', 'Chris', 'Alice', 'Alice', 'Chris')
names(which.max(table(x)))
# 1.9 欠損値の扱いなど
# 欠損値NAへの四則演算
NA + 1
NA - 1
NA * 1
NA / 1
# NAの平均
mean(NA)
# NAと論理演算子
NA == 1
NA != 1
# 数値と論理演算子
a <- 1
a == 1
a != 1
# TRUEは1、Falseは0
TRUE + 1
FALSE + 1
# Rでは欠損値はデフォルトで読み飛ばされる
# NAを１つ含むmath_naで実験
math_na <- scan('clipboard')
mean(math_na)
# mathの先頭にNAを追加して確認→NAになる
math_na2 <- c(NA, math_na)
mean(math_na2)
# 欠損値を削除してNAを無視して計算する
mean(math_na2, na.rm = TRUE)
# 欠損値を見つける
head(is.na(math_na2))
# 1.10 章末問題
# 問題1-1
# 男性の身長の変動係数
5.63/171.58
# 女性の平均身長の変動係数
5.56/158.23
# 男性0.03281268<女性0.03513872より女性のばらつきのほうが大きい
# 問題1-2
# データ
x <- c(2, 5, 11, 7, 9)
# 算術平均
mean(x)
# 幾何平均
prod(x)^(1/length(x))
# 調和平均
1/mean(1/x)
# 不偏分散
var(x)
# 不偏分散の平方根
sd(x)
# 問題1-3
# データ
x <- c(34, 56, 32, 15, 49)
# 平均偏差
mean(x-mean(x))
# 平均偏差
mean(abs(x-mean(x))
# 平均偏差
mean(abs(x-mean(x))
# 平均偏差
mean(abs(x-mean(x)))
# 標準偏差
sqrt(var(x)*(length(x)-1)/length(x))
# 全体の平均点
(math_ave*num)/sum(num)
# 全体の平均点
(math_ave*num)/sum(num)
# データ
math_ave <- c(65, 59, 62)
num <- c(500, 750, 690)
# 全体の平均点
(math_ave*num)/sum(num)
# 全体の平均点
sum(math_ave*num)/sum(num)
height <- c(171.8, 167.2, 180.9)
weight <- c(74.4, 56.3, 93.2)
# BMIの計算
weight/(height^2)
# BMIの計算
weight/(height/100^2)
# BMIの計算
weight/(height/0.01^2)
# BMIの計算
weight/(height*0.01^2)
height <- c(171.8, 167.2, 180.9)*0.01
weight <- c(74.4, 56.3, 93.2)
# BMIの計算
weight/(height^2)
# 問題1-5
# データ
height <- c(171.8, 167.2, 180.9)
weight <- c(74.4, 56.3, 93.2)
# BMIの計算
weight/(height*0.01^2)
height <- c(171.8, 167.2, 180.9)
weight <- c(74.4, 56.3, 93.2)
# BMIの計算
weight/((height*0.01)^2)
# 不偏分散
sqrt(var(x))
# 標準偏差
sqrt(var(x)*(length(x)-1)/length(x))
sd(x)
sqrt(var(x))
sd(x)
sd(x)
# 問題1-6
# データ
x <- c(3, 4, 8, 11, 7)
# 不偏分散の平方根
sqrt(var(x))
sd(x)
# 標準偏差
sqrt(var(x)*(length(x)-1)/length(x))
sd(x)
# 1.4 一変量データの扱い方
# clipboardの内容をmathに格納
math <- scan('clipboard')
# サンプルサイズ
length(math)
#  mathの中身を確認
math
# mathデータの14番目を抽出
math[14]
# mathのヒストグラム（度数表示）
hist(math)
# mathのヒストグラム（相対度数）
hist(math, probability = TRUE)
# mathの平均
mean(math)
# mathの分散（不偏分散）
var(math)
# mathの標準偏差（不偏分散の平方根）
sd(math)
# mathの分散
sqrt(var(math)*(length(math)-1)/length(math))
# 幾何平均
x <- c(1.2, 1.1, 1.05)
prod(x)^(1/length(x))
# 調和平均
v <- c(60, 20)
1/mean(1/v)
1/v
# 1.5 階級数の決め方
# スタージェスの公式
hist(math, breaks = 'Sturges')
# スコットの選択法
hist(math, breaks = 'scott')
# フリードマン＝ダイアコニスの選択法
hist(math, breaks = 'FD')
# 区切り（階級）を指定
hist(math, breaks = 18)
# 区切り（0から100まで10刻み）を指定
hist(math, breaks = seq(0, 100, 10))
# 1.6 Rのグラフをファイルに変換する
# 画像の書き出し
jpeg('math.jpeg', width = 640, height = 480)
hist(math)
dev.off()
# 1.7 分位点と箱ひげ図
# 中央値、メディアン（n=奇数）
score <- c(56, 78, 90)
median(score)
# 中央地、メディアン（n=偶数）
score <- c(56, 78, 81, 90)
median(score)
# 分位点
quantile(math)
# 四分位範囲（IQR）
IQR(math)
# 箱ひげ図
boxplot(math)
# irisデータの表示
iris
# データの先頭部分を表示
head(iris)
# データの末尾を表示
tail(iris)
# データの構造を表示
str(iris)
# 箱ひげ図の表示（１〜３列）
boxplot(iris[, 1:3])
# 箱ひげ図の表示（すべて）
boxplot(iris)
# 箱ひげ図の表示
boxplot(iris[, c(1, 3, 4)])
# 1.8 モード
# モードの表示
names(which.max(table(math)))
# table()について→出現回数の表示
table(math)
# which.max(x)→xの中で最大の値の”位置”を返す
mx <- c(10, 20, 30, 40, 50)
which.max(mx)
# 文字列データで使用
x <- c('Alice', 'Bob', 'Chris', 'Alice', 'Alice', 'Chris')
names(which.max(table(x)))
# 1.9 欠損値の扱いなど
# 欠損値NAへの四則演算
NA + 1
NA - 1
NA * 1
NA / 1
# NAの平均
mean(NA)
# NAと論理演算子
NA == 1
NA != 1
# 数値と論理演算子
a <- 1
a == 1
a != 1
# TRUEは1、Falseは0
TRUE + 1
FALSE + 1
# Rでは欠損値はデフォルトで読み飛ばされる
# NAを１つ含むmath_naで実験
math_na <- scan('clipboard')
mean(math_na)
# mathの先頭にNAを追加して確認→NAになる
math_na2 <- c(NA, math_na)
mean(math_na2)
# 欠損値を削除してNAを無視して計算する
mean(math_na2, na.rm = TRUE)
# 欠損値を見つける
head(is.na(math_na2))
# 1.10 章末問題
# 問題1-1
# 男性の身長の変動係数
5.63/171.58
# 女性の平均身長の変動係数
5.56/158.23
# 男性0.03281268<女性0.03513872より女性のばらつきのほうが大きい
# 問題1-2
# データ
x <- c(2, 5, 11, 7, 9)
# 算術平均
mean(x)
# 幾何平均
prod(x)^(1/length(x))
# 調和平均
1/mean(1/x)
# 不偏分散
var(x)
# 不偏分散の平方根
sd(x)
# 問題1-3
# データ
x <- c(34, 56, 32, 15, 49)
# 平均偏差
mean(abs(x-mean(x)))
# 標準偏差
sqrt(var(x)*(length(x)-1)/length(x))
# 問題1-4
# データ
math_ave <- c(65, 59, 62)
num <- c(500, 750, 690)
# 全体の平均点
sum(math_ave*num)/sum(num)
# 問題1-5
# データ
height <- c(171.8, 167.2, 180.9)
weight <- c(74.4, 56.3, 93.2)
# BMIの計算
weight/((height*0.01)^2)
# 問題1-6
# データ
x <- c(3, 4, 8, 11, 7)
# 不偏分散の平方根
sqrt(var(x))
sd(x)
# 標準偏差
sqrt(var(x)*(length(x)-1)/length(x))
sd(x)
sqrt(mean((x-mean(x))^2))
# 1.4 一変量データの扱い方
# clipboardの内容をmathに格納
math <- scan('clipboard')
# サンプルサイズ
length(math)
#  mathの中身を確認
math
# mathデータの14番目を抽出
math[14]
# mathのヒストグラム（度数表示）
hist(math)
# mathのヒストグラム（相対度数）
hist(math, probability = TRUE)
# mathの平均
mean(math)
# mathの分散（不偏分散）
var(math)
# mathの標準偏差（不偏分散の平方根）
sd(math)
# mathの分散
sqrt(var(math)*(length(math)-1)/length(math))
# 幾何平均
x <- c(1.2, 1.1, 1.05)
prod(x)^(1/length(x))
# 調和平均
v <- c(60, 20)
1/mean(1/v)
1/v
# 1.5 階級数の決め方
# スタージェスの公式
hist(math, breaks = 'Sturges')
# スコットの選択法
hist(math, breaks = 'scott')
# フリードマン＝ダイアコニスの選択法
hist(math, breaks = 'FD')
# 区切り（階級）を指定
hist(math, breaks = 18)
# 区切り（0から100まで10刻み）を指定
hist(math, breaks = seq(0, 100, 10))
# 1.6 Rのグラフをファイルに変換する
# 画像の書き出し
jpeg('math.jpeg', width = 640, height = 480)
hist(math)
dev.off()
# 1.7 分位点と箱ひげ図
# 中央値、メディアン（n=奇数）
score <- c(56, 78, 90)
median(score)
# 中央地、メディアン（n=偶数）
score <- c(56, 78, 81, 90)
median(score)
# 分位点
quantile(math)
# 四分位範囲（IQR）
IQR(math)
# 箱ひげ図
boxplot(math)
# irisデータの表示
iris
# データの先頭部分を表示
head(iris)
# データの末尾を表示
tail(iris)
# データの構造を表示
str(iris)
# 箱ひげ図の表示（１〜３列）
boxplot(iris[, 1:3])
# 箱ひげ図の表示（すべて）
boxplot(iris)
# 箱ひげ図の表示
boxplot(iris[, c(1, 3, 4)])
# 1.8 モード
# モードの表示
names(which.max(table(math)))
# table()について→出現回数の表示
table(math)
# which.max(x)→xの中で最大の値の”位置”を返す
mx <- c(10, 20, 30, 40, 50)
which.max(mx)
# 文字列データで使用
x <- c('Alice', 'Bob', 'Chris', 'Alice', 'Alice', 'Chris')
names(which.max(table(x)))
# 1.9 欠損値の扱いなど
# 欠損値NAへの四則演算
NA + 1
NA - 1
NA * 1
NA / 1
# NAの平均
mean(NA)
# NAと論理演算子
NA == 1
NA != 1
# 数値と論理演算子
a <- 1
a == 1
a != 1
# TRUEは1、Falseは0
TRUE + 1
FALSE + 1
# Rでは欠損値はデフォルトで読み飛ばされる
# NAを１つ含むmath_naで実験
math_na <- scan('clipboard')
mean(math_na)
# mathの先頭にNAを追加して確認→NAになる
math_na2 <- c(NA, math_na)
mean(math_na2)
# 欠損値を削除してNAを無視して計算する
mean(math_na2, na.rm = TRUE)
# 欠損値を見つける
head(is.na(math_na2))
# 1.10 章末問題
# 問題1-1
# 男性の身長の変動係数
5.63/171.58
# 女性の平均身長の変動係数
5.56/158.23
# 男性0.03281268<女性0.03513872より女性のばらつきのほうが大きい
# 問題1-2
# データ
x <- c(2, 5, 11, 7, 9)
# 算術平均
mean(x)
# 幾何平均
prod(x)^(1/length(x))
# 調和平均
1/mean(1/x)
# 不偏分散
var(x)
# 不偏分散の平方根
sd(x)
# 問題1-3
# データ
x <- c(34, 56, 32, 15, 49)
# 平均偏差
mean(abs(x-mean(x)))
# 標準偏差
sqrt(var(x)*(length(x)-1)/length(x))
# 問題1-4
# データ
math_ave <- c(65, 59, 62)
num <- c(500, 750, 690)
# 全体の平均点
sum(math_ave*num)/sum(num)
# 問題1-5
# データ
height <- c(171.8, 167.2, 180.9)
weight <- c(74.4, 56.3, 93.2)
# BMIの計算
weight/((height*0.01)^2)
# 問題1-6
# データ
x <- c(3, 4, 8, 11, 7)
# 不偏分散の平方根
sqrt(var(x))
sd(x)
# 標準偏差
sqrt(var(x)*(length(x)-1)/length(x))
sqrt(mean((x-mean(x))^2))
# 問題1-7
# 得点40点の人の偏差値
z1 <- 50 + 10*(40-mean(math))/sd(math)
# 得点85点の人の偏差値
z2 <- 50 + 10*(85-mean(math))/sd(math)
# 問題1-7
# 得点40点の人の偏差値
z1 <- 50 + 10*(40-mean(math))/sd(math)
# 問題1-7
# 得点40点の人の偏差値
50 + 10*(40-mean(math))/sd(math)
# 問題1-7
math <- scan('clipboard')
# 得点40点の人の偏差値
50 + 10*(40-mean(math))/sd(math)
# 得点85点の人の偏差値
50 + 10*(85-mean(math))/sd(math)
